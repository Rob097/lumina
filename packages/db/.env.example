# @lumina/db — env needed for migrations + seed.
# `pnpm db:migrate` and `pnpm db:seed` read DATABASE_URL.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# Optional: run the integration tests against this DB instead of Testcontainers.
# TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/lumina_test
