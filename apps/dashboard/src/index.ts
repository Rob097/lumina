/**
 * @lumina/dashboard — Next.js 15 merchant control plane.
 *
 * M0 stub. The App Router UI lands in M4. This placeholder imports the shared contract to keep
 * the type-flow wired across the workspace.
 */
import { DEFAULT_LOCALE } from '@lumina/shared';

export const dashboardDefaults = {
  locale: DEFAULT_LOCALE,
} as const;
