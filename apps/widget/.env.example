# @lumina/widget — build-time PUBLIC config only. The widget NEVER receives secrets;
# it only ever sees a site_key, presigned upload URLs, and a generationId at runtime.
PUBLIC_API_URL=http://localhost:3001
PUBLIC_CDN_URL=http://localhost:5173
PUBLIC_SENTRY_DSN=
